# Guess the Bass

Local drum and bass quiz. Load a folder of tracks on this machine, play short clips, and guess the tune. Audio never leaves the browser.

```bash
npm install
npm run dev
```

Open the app, pick a music folder, optionally add players, then start. Chrome or Edge works best for the folder picker.
